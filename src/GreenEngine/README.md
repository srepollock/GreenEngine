# GreenEngine Source Files
This directory is used for the GreenEngine source files.

## Documents / Scratch Pad

`Engine/Engine.cpp`

* Updates `GameObjects` within the `../Project/` dir for the game itself